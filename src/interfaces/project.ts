export default interface Project {
    id: number;
    slug: string;
    title: string;
    description: string;
    image: any;
    url: string;
    createdAt: string;
    updatedAt: string;
    publishedAt: string;
}